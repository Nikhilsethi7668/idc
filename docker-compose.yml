version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: idc-global-events-intelligence
    ports:
      - "3000:80"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
